<% unless @lay_card = System.xml_layout_card(@card, params[:layout]) %>
  <% @lay_content = capture do %>
   {{_main|format;xml}}
  <% end %>
  <% @lay_card = layout_card(@lay_content) %>
<% end %>

<%= render_layout_card(@lay_card) %>
