
<div class="card-header">
  <div class="title-menu">
   	<div class="title">
   		<%= link_to_remote right_arrow+less_fancy_title(card), {:url=>slot.url_for("card/to_view")}, :title=>"open #{card.name}" %>
   	</div>
  	<%= page_icon(card) %>		
  </div>  
</div>    
<%= slot.wrap_content slot.render(:expanded_line_content) %>
