# MySQL (default setup).  Versions 4.1 and 5.0 are recommended.
#
# Install the MySQL driver:
#   gem install postgresql
# On MacOS X:
#   gem install postgresql -- --include=/usr/local/lib
# On Windows:
#   There is no gem for Windows.  Install postgresql.so from RubyForApache.
#   http://rubyforge.org/projects/rubyforapache
#
# And be sure to use new-style password hashing:
#   http://dev.postgresql.com/doc/refman/5.0/en/old-client.html
common: &common
  adapter: mysql
  username: root
  password: password
  host: localhost
  secret: YrDOFOmYJyFg2tTZykCbZjWYQUbKBt


development:
  database: wagn_development
  session_key: wagn_development
  <<: *common

# Warning: The database defined as 'test' will be erased and
# re-generated from your development database when you run 'rake'.
# Do not set this db to the same as development or production.
test:
  database: wagn_test
  session_key: wagn_test
  <<: *common

cucumber:
  database: wagn_test
  session_key: wagn_test
  <<: *common

production:
  database: wagn_production
  session_key: wagn_production
  <<: *common
  
