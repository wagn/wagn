Description:
    The datatype generator creates stubs for a new datatype.

    The generator takes a model name as its argument.  The model name may be
    given in CamelCase or under_score and should not be suffixed with 'Datatype'.

    The generator creates a model class in app/datatypes and a test suite in
    test/unit/datatypes.

Example:
    ./script/generate datatype BookArchive

    This will create a BookArchive datatype:
        Datatype:   app/datatypes/book_archive_datatype.rb
        Test:       test/unit/datatypes/book_archive_datatype_test.rb

