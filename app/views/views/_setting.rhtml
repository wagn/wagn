<div class="setting-row">
	<div class="value">             
		<% if card.ok?(:edit) %>
			<span class="edit-link">
				<%= slot.link_to_action('edit','edit') %>
			</span>
		<% end %>
		<%= slot.render( :closed_content ) %>
	</div>
	<div class="set">
	  <%= Wagn::Pattern.label(card.name.trunk_name)%>
	</div>
</div>