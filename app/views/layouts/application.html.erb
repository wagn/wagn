<% unless @lay_card = System.layout_card(@card, params[:layout]) %>
  <% @lay_content = capture do %>
<!DOCTYPE HTML>
<html>
  <head>
    {{*head}}
  </head>
  
  <body id="wagn">
    <header id="menu">
      [[/ | Home]]   [[/recent | Recent]]   {{*navbox}} {{*account links}}
    </header>
    
    <section id="primary">
      {{_main}}
    </section>
    
    <section id="secondary">
      <div id="logo">[[/ | {{*logo}}]]</div>
      {{*sidebar}}
      <div id="credit">Wheeled by [[http://www.wagn.org|Wagn]] v. {{*version}}</div>
      {{*alerts}}
    </section>
    
    {{*foot}}
  </body>
</html>
  <% end %>
  <% @lay_card = layout_card(@lay_content) %>
<% end %>

<%= render_layout_card(@lay_card) %>