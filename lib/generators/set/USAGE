Description:
    Generates new set files in packs and matching specs.

Example:
    rails generate set standard type tshirt

    This will create:
        pack/standard/sets/type/tshirt.rb
        spec/pack/standard/sets/type/tshirt_spec.rb
