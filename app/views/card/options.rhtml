<%- subtab = params[:attribute] || 'permissions' %>
<% get_slot.wrap(:options) do |slot| %>
	<%= slot.header %>
	<div class="options-body">
    <%= slot.render_partial "card/options/#{subtab}" %>
	</div>
  <%= slot.notice %>
<% end %>
