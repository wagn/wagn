<div class="instruction">
  <%= Wagn::Pattern.label(card.name)%>
</div>
<br/>

<h2>Settings</h2>
<%= slot.subslot(Card::Search.new(:name=>'', 
     :content=>%{{"prepend":"#{card.name}", "type":"Setting"}} #, "left_plus":{"right":"*type"}}}
     )).render :naked %>

<br />
<h2>Cards in Set</h2>
<%= render :partial=> 'types/search/content', :locals=>{ :slot=>slot, :card=>card } %>